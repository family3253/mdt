# 2026-03-08

## 今日关键进展
- 搭建并固化 MDRGNB 自动推送流程：
  - 每日深度简报（当天新发表、去重、Top3、10问总结、四维评分）
  - 每周 Top3 周报
- 新建 skill：`skills/mdrgnb-daily-push/`，并提交到 git。
- 搭建 AI 朝廷骨架：司礼监/兵部/户部/礼部 + 调度模板 + 速用指令。
- QQBot 通道排障完成：从“火星/未送达”恢复到“可收到测试消息”。
- 为“龙虾弟”重绘新版 SVG 头像：深色渐变底 + 橙红龙虾主体 + 赛博蓝高光，已覆盖 `avatars/lobster-avatar.svg`。

## 重要约束
- 不随意删减 `~/.openclaw/openclaw.json`。
- 推送任务使用固定 `to`，且在隔离任务里直接产出最终内容，避免二次 message 调用失败。

## 待持续跟踪
- 观察今晚 MDRGNB 日报双通道稳定性（Feishu/QQ）。
- 后续可加“失败自动重试 + 回执”。

## 追加记录（16:37）
- QQBot 根因定位：曾因配置被覆盖导致鉴权失败；重置 `channels add --channel qqbot --token ...` 后恢复。
- 当前有效定时任务为 4 条：MDRGNB 每日（Feishu/QQ）+ 每周 Top3（Feishu/QQ）。
- 模型体检结论：
  - 可用：`openai-codex/gpt-5.3-codex`
  - 备用：`openai/gpt-5.2-pro`（会话切换可用）
  - 不可用：Gemini 系列（当前 key 报 API_KEY_INVALID）、`gpt-5.3-codex-spark`（账号不支持）
- 已将 fallback 收敛为稳定配置，避免自动切换到不可用模型。
