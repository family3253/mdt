# 2026-03-11

- CPA/CLIProxyAPI 通道模型体检：对 `cpa/gpt-5.4`、`gpt-5.3-codex`、`gpt-5.3-codex-spark`、`gpt-5-codex`、`gpt-5-codex-mini`、`gpt-5.2-codex`、`gpt-5.2` 逐个冒烟，结果 0/7 全失败。
- 现象：CLIProxyAPI `GET /v1/models` 正常返回模型列表，但 `POST /v1/chat/completions` 全部在 25s/40s/65s 后报 `context canceled`，重启 `cliproxyapi.service` 后仍复现。
- 根因确认：抽样直连 OpenAI 官方接口校验多个 `~/.cli-proxy-api/*.json` 账号的 `access_token`，均返回 `401 token_invalidated`。说明不是候选数不够，而是当前 CPA 账号池中的 token 整体已失效。
- 管理接口中的 `candidates=200` 仅表示文件/候选条目存在，不代表 token 仍可实际调用。
- 后续建议：执行 `cpaclean`/`auto_pool_maintainer.py` 做一次正式维护（清理失效账号 + 补号 + 收敛），完成后再复测全模型。
- 用户新增偏好：明确要求“记住 wenxian 任务”，并在 13:56 发起一次 wenxian 推送测试（手动触发 `daily-mdrgnb-wenxian-wecom-0800`，runId: `manual:7d0f3951-650c-4f37-9436-799dc5253ac3:1773208597616:1`）。
- 用户新增执行规则（19:41）：每次下达命令后默认创建后台 subsession 执行，主会话持续对话并汇报进度；遇到问题先自行排障。
- CPA 修复关键经过：先将 Go 升级到本地 `1.26.0`（`~/.local/go/1.26.0`），随后对 `cliproxyapi` 做了“重装级”替换（重新编译二进制、重装 user service、保留备份 `cliproxyapi.reinstall-backup-20260311-125416`），服务维持 `:8080`，代理 `10809`。
- 清理误伤复盘：在 `cpaclean` 执行时，31 个 codex 账号因 `api.openai.com/v1/models` 非 200（主要 403 Missing scopes）被判不健康并删除；当时补号又因邮箱配置错误（`moemail`）失败，导致池子短时仅剩 antigravity。
- 邮箱链路修正：将邮箱 API 修正为 `https://cyc3253.org/` + 有效 key 后，补号链路恢复，可成功创建邮箱并完成部分注册 OAuth（5/5 token 成功补入测试阈值）。
- 关键结论（OAuth scope）：个人号 `codex-komptitgmanchalem@mail.com-plus.json` 账号本身活跃（`wham/usage=200`），但 API 权限缺失（`api.model.read` / `api.responses.write`），导致 `models/responses` 失败。
- 尝试按 issue #24720/#26801 思路在本地 CPA 源码增加 OAuth scopes（`model.request/api.responses.write/api.model.read`）后，OpenAI 授权端返回 `invalid_scope`（客户端不允许请求 `model.request`），说明该 OAuth client 当前不能靠加 scope 直接修复。
- 可用兜底已打通：将用户提供的 OpenAI API key 写入 CPA 的 `codex-api-key` 后，`GET /v1/models` 与 `POST /v1/responses` 均 200，可作为当前稳定主通道（避免继续依赖受限 OAuth scope 路径）。
